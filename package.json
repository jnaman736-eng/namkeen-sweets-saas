{
  "name": "namkeen-sweets-saas",
  "version": "1.0.0",
  "description": "Complete SaaS platform for Namkeen & Sweets manufacturing",
  "private": true,
  "workspaces": [
    "backend",
    "mobile"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspace=backend && npm install --workspace=mobile",
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm start --workspace=mobile\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=mobile"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
